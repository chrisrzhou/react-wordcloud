import * as React from 'react';
import * as d3Cloud from 'd3-cloud';

export interface Props {
  words: string;
  a: number;
}

function Wordcloud(props: Props): React.ReactNode {
  return <div>{props.a}adfadf</div>;
}

export default Wordcloud;
